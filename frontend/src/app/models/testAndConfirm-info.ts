/**
 * model class test and confirm contains the variable needed for our processing
 */
export class TestAndConfirm {
  /**
   * id is stored in this variable
   */
  id: string;
  /**
   * status of the campaign is stored in this variable
   */
  status: string;
}
