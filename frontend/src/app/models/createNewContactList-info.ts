/**
 * this model class contains variable for new contact list
 */
export class ContactListInfo {
  /**
   * name of the contact
   */
  name: string;
  /**
   *email of the contact
   */
  email: string;
  /**
   *default from address of the contact
   */
  defaultFromAddress: string;
  /**
   *our word
   */
  yourWord: string;
  /**
   *global contact list
   */
  globalContactList: string;
}
