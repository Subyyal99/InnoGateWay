/**
 * this model class contains variable for black listing ip data
 */
export class BlackListInfo {
  /**
   * ip address to black list
   */
  ip: string;
  /**
   * msisdn address to black list
   */
  msisdn: string;
  /**
   * reason to black list
   */
  reason: string;
}
